#include "../physics/physics_world.h"

void PhysicsWorld::add_body(RigidBody& rb) {
}

void PhysicsWorld::step() {
    for(uint32_t rb_index = 0; rb_index < bodies.size(); rb_index++) {
        continue;
    }
}